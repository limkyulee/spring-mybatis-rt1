<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyuleelim.admincore.member.mapper.MemberMapper">
    <!--  회원가입  -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (
            username,
            password,
            email,
            role
        )
        VALUES (
            #{username},
            #{password},
            #{email},
            #{role}
        )
    </insert>

    <!--  로그인을 위한 사용자 조회 -->
    <select id="findByEmail" resultType="com.kyuleelim.admincore.member.domain.Member">
        SELECT id, username, email, role
        FROM member
        WHERE email = #{email}
    </select>

    <!-- jwt 토큰 확인을 위한 사용자 전체 조회 -->
    <select id="findAll" resultType="com.kyuleelim.admincore.member.domain.Member">
        SELECT * FROM member
    </select>
</mapper>